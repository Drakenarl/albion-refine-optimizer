# Variables du frontend Vite.
# En dev : NON RENSEIGNE. Le proxy Vite (vite.config.ts) route /api -> :8000.
# En prod : URL du backend Railway/Render, sans slash final.

VITE_API_URL=https://albion-refine-api.up.railway.app
