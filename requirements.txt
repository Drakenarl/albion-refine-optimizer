# Dependances runtime pour Vercel Python (le pyproject.toml n'est pas lu par
# leur builder Python). A garder en sync avec [project.dependencies] +
# [project.optional-dependencies.api] de pyproject.toml.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0
diskcache>=5.6.3
python-dateutil>=2.9.0
typer>=0.12.0
rich>=13.7.0
